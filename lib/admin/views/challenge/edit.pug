extends /common/views/layout.pug
include /challenge/views/mixins/challenge_form

block content
  .section
    h2.text-lg.bold Admin Panel → Challenges →
    h1.text-xl.bold #{challenge.title}
  .section
    +challenge_form(challenge)(action="/admin/challenge/" + challenge.id method="PUT")
